#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main(){
    string name; cin>>name;
    string temp;
    sort(name.begin() , name.end());
    cout<<name;
    for (int i = 0; i < name.size(); i++)
    {
        
    }
    
}